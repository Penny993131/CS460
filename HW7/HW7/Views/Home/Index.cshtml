@model HW7.Models.userprofile

<div class="jumbotron">
    <div class="row">
        <div class="col-lg-4"><img src="@Model.Profileurl" height="150" width="150" /> </div>
        <div class="col-lg-6">
            @Model.Name
            @Model.Login

            @Model.Email
            @Model.Bio


            @Model.Location


            @Model.Company
        </div>
    </div>

</div>

<div id="repos"></div>

<div id="comiitLists">

</div>
@section scripts{
    @Scripts.Render("~/Scripts/main.js")
}